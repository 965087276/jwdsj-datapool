spring:
  application:
    name: datapool-gateway
  profiles:
    active: dev

ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 1000


---
server:
  port: 8888
spring:
  profiles: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1025/eureka/